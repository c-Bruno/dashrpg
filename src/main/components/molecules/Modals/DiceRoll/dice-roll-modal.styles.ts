import { styled } from '@mui/material';

export const ResultBox = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  marginTop: '1rem',
  width: '100%',
  gap: '1rem',
});
