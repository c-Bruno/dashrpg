import { makeStyles } from "@mui/styles";

export const useSkillStyles = makeStyles((theme) => ({
  scrollableBox: {
    overflow: "auto",
    maxHeight: "300px",
    paddingRight: "10px",
    paddingLeft: "20%",
  },
}));