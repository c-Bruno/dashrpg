import { makeStyles } from "@mui/styles";

export const useMasterDiceStyles = makeStyles((theme) => ({
  marginCenter: {
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  dice: {
    cursor: 'pointer',
  },
}));